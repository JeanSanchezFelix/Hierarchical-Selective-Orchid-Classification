experiment_id: species_experts/mobilenet_v2
dataset:
  root_dir: /datasets/taxonomic-orchid
  split_manifest: artifacts/leakage_audit/orchid_split_hash_grouped.csv
  task: genus_species
  # The command replaces this value: --genus Dendrobium
  target_genus: Dendrobium
training:
  model_name: mobilenet_v2
  img_size: 224
  batch_size: 32
  epochs: 10
  learning_rate: 0.001
  optimizer: adam
  criterion: cross_entropy
  data_augmentation: true
  sampler: false
  class_weights: true
